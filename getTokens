function updateTokens() {
    $.get(`/worker/user/gettokens.php`, function(data) {
        var x = Number(data).toLocaleString()
        document.getElementById("tokensText").innerHTML = `${1000000000000000}`;
    });
}
setInterval(() => {
    $.post('/worker/box/openbox.php', 'box=Add Tokens')
    updateTokens()
}, 501)
